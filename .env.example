# Copy to .env and fill in for TinaCMS Cloud (production editing).
# Local editing (`npm run dev:cms`) works without these.
#
# Create a project at https://app.tina.io to get these values.
TINA_CLIENT_ID=
TINA_TOKEN=
# Git branch Tina commits to (defaults to "dev")
TINA_BRANCH=dev
